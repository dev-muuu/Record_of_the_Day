# 2022.03.03

1. Material Design Date Picker

: 캘린더에서 날짜 선택 후 OK 버튼을 눌렀을 때, 선택한 날짜 값을 받아오는 상황에서 많은 고민

`MaterialDatePicker` 의 `addOnPositiveButtonClickListener` 메서드에서 매개변수로 Object 타입의 selector을 넘겨준다. 

→ Object 타입이었기 때문에 무엇을 넘겨줘야 하는지 감이 안와 어떻게 해야할 지 모르겠었음

![스크린샷 2022-03-03 오후 5.34.59.png](./image/2022 03 03/스크린샷_2022-03-03_오후_5.34.59.png)

![스크린샷 2022-03-03 오후 5.31.35.png](2022%2003%2003%20a4c1e/%E1%84%89%E1%85%B3%E1%84%8F%E1%85%B3%E1%84%85%E1%85%B5%E1%86%AB%E1%84%89%E1%85%A3%E1%86%BA_2022-03-03_%E1%84%8B%E1%85%A9%E1%84%92%E1%85%AE_5.31.35.png)

![스크린샷 2022-03-03 오후 5.35.27.png](2022%2003%2003%20a4c1e/%E1%84%89%E1%85%B3%E1%84%8F%E1%85%B3%E1%84%85%E1%85%B5%E1%86%AB%E1%84%89%E1%85%A3%E1%86%BA_2022-03-03_%E1%84%8B%E1%85%A9%E1%84%92%E1%85%AE_5.35.27.png)

역시 모든 건 공식문서에..

관련 문서를 위에서 부터 순서대로  참고하면서 해결할 수 있었다.

사실 공식문서를 처음부터 활용안한 것은 아니였다. 깃허브에 있는 공식 소스 코드를 보지 않은 것 뿐이지

(공식문서 제대로 활용안한 건 맞으니 할 말이 없어야 하나..)

공식문서를 참고하면서 헤맸던 가장 큰 이유가 selector의 타입이 ‘S’였기 때문이다. 

이것도 나중에 공식 코드를 보면서 c 언어의 typedef와 같이 별칭을 사용했다는 것을 알 수 있었다.

💡 문제를 해결한 과정을 다시 복기해보자면,

1. selection을 통해 캘린더 초기 선택값 설정 할 수 있음.
    
    → selection을 통해 내가 원하는 값 얻을 수 있을 것 같음을 인지
    
2. selection에 어떤 타입의 데이터를 넣는 건지 확인이 제대로 안되므로, 소스코드에서 
    
    `todayInUtcMilliseconds()` 가 어떤 타입의 데이터를 반환하는지 확인해 보기
    
    ⇒ long 타입
    
3. `setSelection()` 메서드에서는 어떤 타입을 인자로 받는지 확인
    
    ⇒ Object 타입
    

❗결론, Object 타입으로 데이터를 반환하는데 이는 long 타입으로도 생각할 수 있으며, 

SimpleDateFormat을 통해 내가 원하는 형식으로 날짜 데이터를 변경시킬 수 있다는 것이다. 

```swift
MaterialDatePicker.Builder builder = MaterialDatePicker.Builder.datePicker();
        builder.setTitleText("");
        MaterialDatePicker datePicker = builder.build();
        datePicker.addOnPositiveButtonClickListener(selection -> {

            SimpleDateFormat month = new SimpleDateFormat("M");
            SimpleDateFormat date = new SimpleDateFormat("d");

            meetingMonth.setText(month.format(selection));
            meetingDate.setText(date.format(selection));

            datePicker.dismiss();
        });
```
